---
description: Pre-flight allow/deny/clarify gate for high-risk tool calls — writes, deletes, deploys, IAM/policy edits, secret operations, broad-scope automation, prompts asking humans for sensitive data. Activates on those topics and on any agent action that mutates state outside the workspace.
globs:
alwaysApply: false
---

# Intent judge (thin trigger)

Before high-risk mutations (writes, deletes, deploys, IAM/policy edits, secret ops), follow [`skills/intent-judge-shim/SKILL.md`](../skills/intent-judge-shim/SKILL.md).

- **MCP**: `intent-judge` in [`.cursor/mcp.json`](../mcp.json) → `skills/intent-judge-shim/intent_judge_mcp_server.py`

Claude Code hook porting is documented in [`docs/multi-tool-config.md`](../../docs/multi-tool-config.md); not implemented in-repo.
