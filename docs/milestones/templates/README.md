# Milestone templates

Version-controlled milestone templates for [ADR-013](../ADR/ADR-013-guided-milestone-orchestration.md).

Each template is a YAML or Markdown file in this directory (to be added per `template_id`).
Templates define issue title patterns, labels, body sections, optional child issues, and approval gates.
