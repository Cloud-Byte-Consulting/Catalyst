# Golden Path — {{golden_path_name}}

**Target persona**: {{target_persona}}
**Linked module**: {{linked_module}}
**Status**: published
**Owner**: <!-- team / on-call rota -->
**Last reviewed**: {{date_iso}}

> Generated by `.cursor/skills/aws-platform-engineering` capability
> `generate-golden-path-doc` per ADR-005 G-11.
>
> The five sections below (What / Why / Opinion / Paved road / Escape hatch)
> are non-negotiable per the AWS IDP guide
> (https://docs.aws.amazon.com/prescriptive-guidance/latest/internal-developer-platform/introduction.html).

## What

<!-- One paragraph defining the offering. Plain language. The developer
should know whether this golden path applies to them after reading this
section. -->

## Why

<!-- The developer pain this golden path removes. Frame in cognitive-load
terms (the IDP guide's vocabulary): what does the developer NOT have to
learn / decide / remember now that this golden path exists? Cite at least
one prior ticket / PR / Slack thread that motivated the work. -->

## Opinion

<!-- The team's stance. What is enforced. What is configurable. What is
out-of-scope. Honest about the trade-offs you took. Examples:
- "Defaults to ECS Fargate; EKS is out-of-scope for this path."
- "Multi-AZ HA is enforced in prod; single-AZ allowed in dev."
- "OIDC is the only sanctioned CI auth method." -->

## Paved road

<!-- Step-by-step the developer follows. Concrete commands and file paths.
Should be reproducible end-to-end without external help.

Example structure:

1. Create the issue
   ```bash
   gh issue create --template kaizen --title "..." \
     --label "type/kaizen,tenant/catalyst,env/<env>,..."
   ```
2. Create the branch
   ```bash
   git switch -c rc/my-feature
   ```
3. Generate the module from the skill
   ```
   @aws-platform-engineer generate <something> for <my use case>
   ```
4. Plan / apply via OIDC CI
   - PR triggers `terraform plan`; review; merge to `release`.
   - `release` push triggers `terraform apply` via the OIDC `apply` role.
5. Verify
   ```bash
   curl https://<service-host>/healthz
   ```
-->

## Escape hatch

<!-- When the paved road doesn't fit, what is the supported way to diverge?
Default escape hatch: open a `type/kaizen` describing the unmet need; the
platform team triages it weekly and decides whether to extend the golden
path or document the divergence as a workload-specific exception.

If a divergence is sanctioned, capture it in an ADR (use the
`generate-adr` capability) so future agents see the precedent. -->

## Owner / on-call / SLOs

| Field | Value |
|---|---|
| Owner team | <!-- team-name --> |
| On-call rota | <!-- pagerduty / opsgenie schedule --> |
| Documentation freshness SLO | reviewed at least quarterly |
| Production incident response | acknowledged within 30m, mitigated within 4h |

## Source citations

- AWS IDP guide: https://docs.aws.amazon.com/prescriptive-guidance/latest/internal-developer-platform/introduction.html (`AWS-prescriptive`)
- ADR-005: [`docs/ADR/ADR-005-aws-agentic-platform-engineering.md`](../ADR/ADR-005-aws-agentic-platform-engineering.md)
- Linked module: {{linked_module}}
